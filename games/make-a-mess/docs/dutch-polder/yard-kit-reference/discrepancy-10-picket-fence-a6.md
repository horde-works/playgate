# Discrepancy pass 10 — Zaan picket fence and gate, revision a6

## Fixed-camera review

- `picket-fence-front`: module rhythm, grounded posts, green/white hierarchy and the declared gate envelope read correctly.
- `picket-fence-three-quarter`: board thickness, open gaps and the shared hinge post read correctly.
- `picket-fence-gate-detail`: revealed a hidden construction error. The a6 hinge pins and straps sat `5–27 mm` in front of the gate/post faces; latch iron was similarly detached in `Z`. The diagonal brace stopped `7.5 mm` before the gate-frame front face.

## Correction owned by revision a7

- Move hinge pin axes and straps back to intersect the shared post and gate leaf.
- Move latch bar and catch back to intersect both the gate frame and latch post; retain a separate handle that intersects the bar.
- Move the diagonal back until it overlaps the white frame and the infill plane.
- Extend tests from endpoint checks to independent depth-overlap checks.

No accepted schouw, mooring-post, jetty or yard-shed geometry is changed by this pass.
